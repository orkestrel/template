/** Options for `createTemplate`. */
export interface TemplateOptions {
	readonly id?: string
}

/** A working `Template` — pure data, no behavior. */
export interface TemplateInterface {
	readonly id: string
}
